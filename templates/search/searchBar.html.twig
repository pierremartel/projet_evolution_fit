{% extends 'base.html.twig' %}

{% block title %}Hello SearchController!{% endblock %}


{% block body %}

    {# {{ form(form) }} #}

    {# {% for product in products %}
        <p>{{ product.name }}</p>
    {% endfor %} #}

{% endblock %}


